import React from 'react';
import { withRouter } from 'react-router-dom';

function CreateListing<RouteComponentProps>() {
    return ( 
        <div>Create a new listing.
      </div>
    )
}

export default withRouter(CreateListing);